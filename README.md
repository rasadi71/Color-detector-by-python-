# 🎨 Color Detection using Python (Red, Green, Yellow)

This project detects **Red**, **Green**, and **Yellow** colors in real-time using **OpenCV** and a webcam feed.  
Bounding boxes and labels are drawn around the detected colors.

---

## 🧠 Requirements

Make sure you have the following installed:

```bash
pip install opencv-python numpy
